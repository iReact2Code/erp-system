// This file is no longer needed since we switched to JWT authentication
// Keeping it as a placeholder to avoid breaking imports

export const authConfig = {
  // Empty config - using JWT authentication instead
}
